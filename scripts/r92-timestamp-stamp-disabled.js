// R92_LEGACY_TIMESTAMP_STAMP_DISABLED
// The r74-r90 per-segment stamp owner is intentionally inert in r92.
// Exact timestamps are rendered only by the Transfer-owned overlay root.

// Compatibility sentinel for the inherited r86/r78 generated verifier.
// It is a comment only: no function body is restored and stampSegment stays inert.
// function isFinalAssistantSurface(segment) {

  function stampSegment() {
    return;
  }
